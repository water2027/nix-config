{
  wayland.windowManager.hyprland.settings = {
    decoration = {
      rounding = 0;

      # 窗口透明度
      active_opacity = 1.0;
      inactive_opacity = 1.0;

      # 阴影配置
      shadow = {
        enabled = true;
        range = 4;
        render_power = 3;
        color = "rgba(1a1a1aee)";
      };

      # 模糊配置
      blur = {
        enabled = true;
        size = 3;
        passes = 2;
        vibrancy = 0.1696;
      };
    };
  };
}